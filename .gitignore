*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Local tooling / editor state
.claude

# Not part of the current handover — held back deliberately, not deleted.
# Re-enable by removing the matching line when these are ready to ship.
/.github/
/docs/
README_bkp.md
readme_bkp.md

# Generated from the controller annotations + APP_URL, so its contents are
# machine-specific. `composer setup` / `composer docs` rebuilds it.
/storage/api-docs/
